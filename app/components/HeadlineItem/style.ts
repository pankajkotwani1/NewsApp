import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    padding: 16,
    borderRadius: 8,
    marginBottom: 18,
  },
  headline: {
    fontSize: 16,
    color: '#000',
    fontFamily: 'verdana',
  },
  pinned: {
    fontSize: 12,
    color: 'green',
  },
});

export default styles;
